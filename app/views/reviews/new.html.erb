<div class="container">
  <h1>Add a Review</h1>
  <!-- The form builds the _path -->
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.submit class: 'btn btn-success' %>
  <% end %>
</div>
